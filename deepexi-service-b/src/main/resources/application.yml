server:
  port: 8002

spring:
  application:
    name: deepexi-service-customer

  http:
    encoding:
      charset: UTF-8

eureka:      # 将服务注册到注册中心
  client:
    register-with-eureka: false
    #fetch-registry: false
    service-url:
      defaultZone: http://localhost:8003/eureka #多个时，逗号分隔
    #enabled: true
